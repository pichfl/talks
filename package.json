{
	"name": "talks",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"format": "biome check --write . && prettier --write **/*.{html,js,css,md}",
		"lint": "biome check . && prettier **/*.{css,md}"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"dependencies": {
		"auto-reveal": "0.3.0",
		"auto-reveal-theme-fp": "0.0.2",
		"biome": "^0.3.3"
	},
	"devDependencies": {
		"prettier": "^3.3.3"
	}
}
